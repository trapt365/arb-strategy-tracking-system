# Пачка 8.2–8.4: заметки исполнения (облегчённый цикл Epic 8)

Status: done  # 2026-07-08: dev пачкой + одно общее ревью пройдено, 5 MED-находок закрыто; 538/538 vitest, tsc, canary --no-claude PASS. Осталось: Manual TODO BotFather (8.2) + прод-деплой пачки.

> Story-файлы для 8.2–8.4 не создаются (облегчённый цикл). Этот файл — носитель
> Manual TODO, отметок регресса по-story и записи общего ревью.

## Story 8.2: Ребрендинг и аудит текстов

Сделано (код):
- W1: `formatWelcomeMessage` актуализирован — упоминает онбординг (`/newclient`, `/draft`,
  `/resume`, `/skip`, `/confirm`), `/status` убран из «Скоро» (остались только
  нереализованные 🔍 Найти и 📋 Повестка).
- W7: тексты инварианта 1 синхронизированы с ослабленным `/confirm` («предупреждает,
  не блокирует»): черновик (`f0-onboarding.ts` renderOkrSection), чеклист `/status`
  (`f0-client-card.ts` action), welcome.
- W8: имена людей Geonline убраны из примеров и дефолтов:
  - пример расписания `f0-fill.ts` → «Айгерим — вт 15:00, Бекзат — чт 11:00»;
  - `DEFAULT_TOP_NAME='Жанель'` → `GEONLINE_DEFAULT_TOP_NAME`, применяется ТОЛЬКО при
    `clientId==='geonline'`; для остальных fallback = название компании из реестра
    (`getClientName`, новый) → «Клиент»;
  - промпты: `analysis.md` / `format-tracker.md` shape-примеры («Алия»),
    `f0-full-extraction.md` «GeOnline BSC-версии» → нейтрально; CHANGELOG v1.4.1.
- Обратная совместимость: путь `clientId==='geonline'` не менялся (fallback Жанель
  сохранён именно для него), `GEONLINE_F0_SHEET_ID` не тронут.

### Manual TODO (Тимур) — профиль бота в BotFather

1. `/mybots` → @geonline_report_bot → **Edit Name** → «AI-трекинг бот» (или «Трекинг-бот
   практики» — на вкус; главное без «Geonline»).
2. **Edit Description** (видна при первом открытии бота):
   «Онбординг стратегии клиента и автоотчёты по трекшн-встречам. Онбординг: /newclient.
   Отчёт по записи: /report <ссылка>.»
3. **Edit About** (короткая, в профиле): «AI-ассистент трекинг-практики: онбординг + отчёты».
4. **Botpic** — опционально нейтральная иконка.
5. ⚠️ **Username @geonline_report_bot сменить НЕЛЬЗЯ** (ограничение Telegram).
   Развилка (решение отложено, не блокирует 8.2):
   - оставить username как есть (виден только в ссылке t.me/…; display name нейтральный), ИЛИ
   - завести нового бота с нейтральным username + перенести токен: правки `TELEGRAM_BOT_TOKEN`
     в prod `.env`, тот же `TELEGRAM_TRACKER_CHAT_IDS` (chat_id трекеров не меняются, но каждому
     надо нажать Start у нового бота), persisted-сессии по chatId выживают. Делать в отдельное окно.

## Story 8.3: Читаемая доставка + честный прогресс

Сделано (код):
- W4 (F0): `renderF0DraftSummaryMessage` — компактный черновик: счётчики (цели/KR/гипотезы/
  участники) + блоки 🔴 (KR cap 10, гипотезы списком) + «Не распознано» (cap 5) + обещание
  ссылки после `/confirm`. Полные таблицы НЕ инлайнятся (они в Sheets). Полный рендер
  `renderF0FullDraftMessage` сохранён для смоуков.
- W2 (F0): честный прогресс сборки — оценка по размеру пакета (≤20k знаков «1–2 мин»,
  ≤60k «2–5 мин», больше «5–12 мин»); progress-сообщение живёт до результата: тикер
  editMessage каждые 45 с («прошло N мин, потолок 12 минут»), по завершении редактируется
  в итог, при ошибке — в текст ошибки. Молчаливый deleteMessage убран.
- W4 (F1): `formatDeliveryReportCompact` — короткие отчёты без изменений; длинные (>4000)
  → компакт: секции усечены (8 строк/700 симв.), commitments cap 8, footer «отчёт сокращён»
  + ссылка на таблицу клиента (sheetId из реестра/config-fallback). splitForTelegram
  страхует лимит 4096 в любом случае. Partial-fallback не сжимается (диагностика).
- Гигиена: `data/.onboarding/` и `data/clients/` в .gitignore (runtime, создаются и тестами).
- Тесты: bot.test.ts — новый harness F0-флоу (getFile-стаб, documentUpdate, commandUpdate,
  F0-deps в buildBot) + 4 теста (честная оценка big/small, итог/ошибка редактируются в
  progress, компакт без таблиц); formatter — 4 теста compact; f0-onboarding — 3 теста summary.

## Story 8.4: Стартовое меню, навигация, защита сессии

Сделано (код):
- W1: `/start` = welcome (8.2) + inline-меню: «ℹ️ Что умеет бот» / «🆕 Онбординг нового
  клиента» / «👥 Клиенты» (callbacks `menu:help|new|clients`).
- W10: «Клиенты» → список из реестра (`loadRegistry`), клик `client:{id}` → карточка из
  `data/{id}/card.json` (`renderClientCardMessage`: участники/расписание/старт + мини-чеклист
  по полям карточки; полнота KR — в панели таблицы) — работает и для завершённых клиентов;
  без карточки — внятный fallback. Кнопка «✅ Работать с этим клиентом» (`client_use:{id}`)
  → выбор персистится в `data/clients/active-clients.json` (переживает рестарт — иначе
  /report молча уехал бы в geonline); `/report` без clientId идёт по активному клиенту
  (явный аргумент приоритетнее; протухший активный → warn + geonline-fallback).
  `/status` без сессии показывает карточку активного клиента.
- W3: защита сессии — `/newclient`, кнопка меню и текст-триггер «новый клиент» идут через
  guard: при filling или collecting-с-файлами бот просит подтверждение
  (`f0_new_yes`/`f0_new_no`) с компанией и прогрессом «отвечено X из Y». `/cancel` —
  завершение сессии с подтверждением (`f0_cancel_yes`/`f0_cancel_no`), чистит память+диск.
- W9: `setMyCommands` + resume, skip, cancel.
- Регресс: путь geonline не менялся (без активного клиента /report → geonline + Жанель —
  закреплено тестом).
- Тесты: +9 (меню, список/карточка/фолбэк, client_use+persist+/report-резолв,
  geonline-регресс, guard yes/no, /cancel) + обновлён lifecycle setMyCommands.

## Общее ревью пачки (2026-07-08, свежий контекст по диффу 2cc178f^..c0dc5e8)

Исправлено (5 MED):
1. Гонка прогресс-тикера: тик (или его in-flight edit) мог затереть доставленное саммари
   черновика обратно на «Собираю…» → флаг `finished` + `clearInterval` строго ДО
   финального edit в `finishProgress`.
2. callback_data > 64 байт: `clientIdFromCompany` не капил длину слага — одна длинная
   компания молча ломала весь список «Клиенты» (400 BUTTON_DATA_INVALID на sendMessage)
   → кап 32 символа (`CLIENT_ID_MAX_LENGTH`) + защитный skip+warn при построении меню.
3. Протухшие кнопки подтверждения обходили W3-guard: старая «Да, сбросить»/«Да, завершить»
   убивала НОВУЮ сессию → session.id в callback_data (`f0_new_yes:{sid}`,
   `f0_cancel_yes:{sid}`) + сверка с текущей сессией («⌛ Кнопка устарела»).
4. Отмена во время сборки воскресала: buildF0Draft по завершении персистил отменённую
   сессию → проверка `f0Sessions.get(chatId) === session` перед сохранением результата
   (+ /cancel при processing отвечает BUSY).
5. geonline отсутствовал в меню «Клиенты» — активный выбор нельзя было вернуть на
   встроенного пилота без «/report <url> geonline» → строка «Geonline (встроенный пилот)».
Плюс LOW: усечение секции компактного F1-отчёта могло разрезать суррогатную пару
(эмодзи на границе 700) → drop lone surrogate.

Принято без фикса (LOW, задокументировано):
- Компактный F1-отчёт при очень многих коротких секциях может превысить 4000 —
  splitForTelegram страхует лимит 4096, деградация корректная.
- `setActiveClient` — read-modify-write без сериализации: при одном пользователе-трекере
  риск потери записи ничтожен.
- Тесты 8.4 бэкапят реальные data/clients/*.json в памяти раннера: крэш между
  beforeEach/afterEach потеряет dev-файл (прод не гоняет тесты).

Вердикт ревью по AC: 8.2 met (canary/golden — прогнаны в --no-claude режиме; semantic
canary с Claude — по желанию перед пилотом, промпт-правки только shape-примеры);
8.3 met (гонка тикера закрыта); 8.4 met (64-байта, stale-кнопки, geonline-в-меню закрыты;
«полнота KR» в карточке — задокументированное упрощение, оценка живёт в панели таблицы).
