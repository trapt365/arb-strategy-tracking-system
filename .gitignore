node_modules/
dist/
.env
.env.*
!.env.example
*.log
.DS_Store
*.code-workspace
.vscode/

# Credentials / service-account keys (never commit secrets)
data/google-service-account.json
**/*service-account*.json

# Test audio files (large binaries)
data/test-audio/
data/golden/
data/soniox-results/
data/prompt-results/

# Weekly meeting audio (sensitive — never commit raw recordings)
data/week-*/

# Harness state
.claude/scheduled_tasks.lock

# Story 1.10: runtime client data + scheduler/ops state
data/.ops-state.json
data/.scheduler-state.json
data/.backups/
data/geonline/
# Story 8.3: runtime F0-онбординг (черновики + persisted-сессии; создаются и тестами)
data/.onboarding/
data/clients/

# Story 1.11: canary run outputs (developer artifact for post-mortem)
data/canary-results/
.bmad-loop/runs/
.bmad-loop/cache/

# BMAD personal settings (never commit)
_bmad/config.user.yaml
