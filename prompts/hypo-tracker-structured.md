Ты аналитик стратегического трекинга. Тебе дана информация о гипотезах клиента {{clientName}} за неделю {{weekNumber}}.

Группы гипотез по департаментам (JSON):
{{deptGroupsJson}}

Тексты F1-отчётов за текущую неделю (могут быть пустыми):
{{f1ReportsText}}

Твоя задача — для каждой гипотезы из массива написать краткий комментарий, основанный на динамике статуса и фактах из F1-отчётов. Для новых гипотез (isNew=true) добавь рекомендации по запуску (launch), ожидаемому результату (result) и следующему шагу (nextStep). Также сформулируй 3–5 ключевых топ-инсайтов по всем гипотезам.

Правила:
- comment: стрелка динамики + прямая речь/факт из F1-отчёта (если есть). Если данных нет — оставь пустой строкой.
- launch, result, nextStep: только для новых гипотез; если нет данных — опусти поле.
- topInsights: 3–5 пунктов, качественные выводы о динамике, рисках, паттернах.
- Не повторяй статистику в topInsights — только качественные наблюдения.
- statement в ответе должен точно совпадать со statement из входных данных.

Ответь строго в формате JSON без markdown-фенсов:
{
  "hypoInsights": [
    {"statement": "...", "comment": "...", "launch": "...", "result": "...", "nextStep": "..."}
  ],
  "topInsights": ["инсайт 1", "инсайт 2", "инсайт 3"]
}
